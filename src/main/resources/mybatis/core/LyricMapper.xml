<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kxai.qms.core.dao.LyricDao">

    <!-- 根据音乐ID查找列表 -->
    <select id="findListByMusicId" parameterType="int" resultType="Lyric">
        SELECT
            lyric_id   AS lyricId,
            lyric_name AS lyricName,
            music_id   AS musicId,
            lyric_uri  AS lyricUri,
            lyric_ext  AS lyricExt
        FROM qms_lyric
        WHERE music_id = #{musicId}
    </select>

    <!-- 根据音乐ID删除歌词 -->
    <delete id="deleteByMusicId" parameterType="int">
        DELETE FROM qms_lyric
        WHERE music_id = #{musicId}
    </delete>

    <!-- 保存 -->
    <insert id="save" parameterType="Lyric">
        INSERT INTO qms_lyric (lyric_id, lyric_name, music_id, lyric_uri, lyric_ext, create_time) VALUES (#{lyricId}, #{lyricName}, #{musicId}, #{lyricUri}, #{lyricExt}, #{createTime})
    </insert>

    <!-- 根据歌词ID删除歌词 -->
    <delete id="deleteByLyricId" parameterType="int">
        DELETE FROM qms_lyric
        WHERE lyric_id = #{lyricId}
    </delete>

    <!-- 根据歌词ID查找 -->
    <select id="findByLyricId" parameterType="int" resultType="Lyric">
        SELECT
            lyric_id   AS lyricId,
            lyric_name AS lyricName,
            music_id   AS musicId,
            lyric_uri  AS lyricUri,
            lyric_ext  AS lyricExt
        FROM qms_lyric
        WHERE lyric_id = #{lyricId}
    </select>

    <!-- 修改 -->
    <update id="update" parameterType="Lyric">
        UPDATE qms_lyric
        SET lyric_name = #{lyricName}, music_id = #{musicId}, lyric_uri = #{lyricUri}, lyric_ext = #{lyricExt}
        WHERE lyric_id = #{lyricId}
    </update>
</mapper>